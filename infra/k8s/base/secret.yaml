# Replace via SealedSecrets / External Secrets / overlay — never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: docflow-secrets
type: Opaque
stringData:
  SECRET_KEY: REPLACE_ME
  DATABASE_URL: postgresql+psycopg://docflow:REPLACE_ME@postgres:5432/docflow
  POSTGRES_PASSWORD: REPLACE_ME
  SMTP_HOST: ""
  SMTP_USER: ""
  SMTP_PASSWORD: ""
  SMTP_FROM: DocFlow <noreply@example.com>
